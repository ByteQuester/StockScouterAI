# Extract available metrics (keys) from the loaded data

# Allow users to select metrics to display

# Filter data based on selected metrics

# Update the Nivo Line chart data in the Streamlit Elements dashboard
